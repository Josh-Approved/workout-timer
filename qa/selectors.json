{
  "$comment": "Selector registry — the one place FWT's brittle UI strings live. Flows reference @keys, never raw copy. testID preferred (copy-proof); text is a Maestro regex matching visible text OR a11y label. Anchors target the seeded 'Standard Tabata' QA fixture (src/qa/fixtures.ts).",
  "anchors": {
    "app-title": {
      "text": "Workout timer",
      "note": "TimerList header title (i18n app.title, sentence case) — was 'Workout Timer'/'Free workout timer' before the retired-'Free' cleanup; asserting ONE title on both platforms is the parity regression guard"
    },
    "edit-tabata": {
      "text": "Standard Tabata, edit",
      "note": "edit affordance on the seeded Standard Tabata timer. Text = the row's a11y label, which leads with the timer name (was 'Edit Standard Tabata' until 2026-09-10, defect workout-timer-20260809-1: Voice Control matches the accessible name against the words a user can SEE, and the row prints the name first)"
    },
    "save-timer": {
      "text": "Save timer"
    },
    "back": {
      "text": "Back"
    },
    "start-tabata": {
      "text": "Start Standard Tabata"
    },
    "exercise": {
      "text": "Exercise",
      "note": "mid-workout phase label"
    },
    "stop-workout": {
      "text": "Stop workout"
    },
    "stop-confirm": {
      "text": "Stop",
      "note": "confirm button in the system stop alert"
    },
    "settings": {
      "text": "Settings"
    },
    "send-feedback": {
      "text": "Send feedback",
      "note": "Settings 'Send feedback' row (i18n about.feedback) — opens the canonical feedback sheet"
    },
    "feedback-bug-option": {
      "text": "Report a bug.*",
      "note": "feedback sheet type option (i18n feedback.type.bug). Trailing .* matches the iOS merged a11y label — the card's Pressable sets accessibilityLabel `${title}. ${desc}`, so a bare full-match never hits (the documented iOS card-merge convention)"
    },
    "create-new": {
      "text": "Create new timer",
      "note": "the timer-list create affordance (i18n timerList.createNew) — opens the editor with a blank EMPTY_TIMER (valid defaults, so Save succeeds with just a name). Used by the T4 survival flow."
    },
    "name-input": {
      "text": "Timer name",
      "note": "the editor name TextInput (i18n editor.nameA11y). A real RN TextInput — in the a11y tree on both platforms."
    },
    "survival-timer": {
      "text": ".*Chaos Interval.*",
      "note": "T4 survival marker — a created timer name absent from the seed, so assertNot holds before create and assert can only pass if the new timer persisted. .* for the iOS merged card a11y label."
    },
    "upgrade-timer": {
      "text": ".*Legacy Interval.*",
      "note": "T5 upgrade-harness marker — a timer name absent from the QA seed, written by the LAST-RELEASED binary so the assert run can prove it survived installing HEAD over it. Distinct from @survival-timer so a failure names which harness wrote the datum."
    },
    "sound-work-row": {
      "text": "Exercise start: Voice",
      "note": "the Exercise-start sound DrilldownRow a11y label (default style Voice) — `label: value`"
    },
    "sound-style-chime": {
      "text": "Ascending Chime",
      "note": "a style option that only exists inside the SoundStyleSheet (no row shows it by default) — proves the sheet opened"
    },
    "sound-style-bell": {
      "text": "Bell",
      "note": "the style the flow picks; sheet stays open (audition semantics), Back closes"
    },
    "sound-work-row-bell": {
      "text": ".*Exercise start: Bell.*",
      "note": "the settings row after the pick — the Tier-2 outcome proof the spoke wrote through"
    },
    "edit-climbing": {
      "text": "Climbing Hangboard, edit",
      "note": "edit button on the seeded climbing timer (fixtures qa-hangboard) — sources the store climbing screenshot slot. Name-first for the same reason as @edit-tabata (was 'Edit Climbing Hangboard')"
    }
  }
}
