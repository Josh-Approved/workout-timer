name: QA e2e

on:
  pull_request:

jobs:
  ios:
    if: hashFiles('app.json') != ''
    runs-on: macos-14
    timeout-minutes: 45
    env:
      EXPO_PUBLIC_QA_MODE: '1'
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: npm
      - name: Install Maestro
        run: |
          curl -Ls "https://get.maestro.mobile.dev" | bash
          echo "$HOME/.maestro/bin" >> $GITHUB_PATH
      - name: Install xcbeautify
        run: brew install xcbeautify
      - name: npm ci
        run: npm ci
      - name: Expo prebuild (ios)
        run: npx expo prebuild --platform ios --clean --no-install
      - name: Cache CocoaPods
        uses: actions/cache@v4
        with:
          path: ios/Pods
          key: pods-${{ hashFiles('ios/Podfile.lock') }}
          restore-keys: pods-
      - name: pod install
        working-directory: ios
        run: pod install
      - name: Detect Xcode workspace + scheme
        id: xcode
        run: |
          WS=$(ls ios | grep '\.xcworkspace$' | head -1)
          SCHEME="${WS%.xcworkspace}"
          echo "workspace=ios/$WS" >> $GITHUB_OUTPUT
          echo "scheme=$SCHEME" >> $GITHUB_OUTPUT
      - name: Build for iPhone simulator
        run: |
          set -o pipefail
          xcodebuild \
            -workspace "${{ steps.xcode.outputs.workspace }}" \
            -scheme "${{ steps.xcode.outputs.scheme }}" \
            -configuration Debug \
            -sdk iphonesimulator \
            -derivedDataPath build \
            -destination "generic/platform=iOS Simulator" \
            CODE_SIGNING_ALLOWED=NO | xcbeautify
      - name: Boot iPhone 16 Pro Max + normalize status bar
        run: |
          xcrun simctl boot "iPhone 16 Pro Max" || true
          xcrun simctl bootstatus "iPhone 16 Pro Max" -b
          xcrun simctl status_bar "iPhone 16 Pro Max" override \
            --time "9:41" \
            --batteryState charged --batteryLevel 100 \
            --cellularBars 4 --wifiBars 3
      - name: Install app on simulator
        run: |
          APP_PATH=$(find build -name "*.app" -type d -path "*/Debug-iphonesimulator/*" | head -1)
          echo "App path: $APP_PATH"
          xcrun simctl install booted "$APP_PATH"
      - name: Run Maestro flow
        run: |
          mkdir -p qa/captures/ios
          maestro test qa/flows/mobile.yaml --env=PLATFORM=ios --env=STORE=ios
      - name: Frame screenshots
        run: node scripts/render-screenshots.mjs --store ios
      - name: Upload iOS framed store assets
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: store-assets-ios
          path: |
            store-assets/screenshots/ios/
            qa/captures/ios/
